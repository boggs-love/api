framework:
    messenger:
        adapters:
          rsvp:
            dsn: "%env(MESSENGER_ADAPTER_DSN)%"
            options:
              auto-setup: 'false'
              exchange:
                name: rsvp
        routing:
          'App\Entity\RSVP': messenger.sender.rsvp
